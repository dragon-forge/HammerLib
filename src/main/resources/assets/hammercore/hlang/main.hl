//we import HammerCore from java and transform it's name to Hammer_Core
import: com.zeitheron.hammercore.HammerCore:Hammer_Core;

hook PreInit
{
	msg = str("Hammer Lang: PreInit hook!");
	Hammer_Core.LOG.info(msg);
}